fastapi>=0.115
uvicorn[standard]>=0.32
SQLAlchemy>=2.0
bcrypt>=4.0
python-jose[cryptography]>=3.3
python-multipart>=0.0.9
pydantic[email]>=2.9
# Lo necesita la exportacion del resumen del taller, que corre DENTRO de la API.
# El importador tambien lo usa, pero ese se ejecuta a mano y por eso falto aqui
# hasta que el contenedor se nego a arrancar.
openpyxl>=3.1
